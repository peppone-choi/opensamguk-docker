# 실제 환경변수/비밀값 — 절대 커밋 금지
.env
.env.*
!.env.example

# 서버별 env(실제 비밀값) — *.example만 커밋
servers/*.env
!servers/*.env.example

# 런타임 TLS 인증서 — 운영 호스트에서만 생성·갱신, 절대 커밋 금지
infra/nginx/certs/

# OS / 에디터
.DS_Store
*.swp
